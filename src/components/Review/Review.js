import React from 'react';

const Review = (props) => {
    const {name, review, rating, img} = props.review;
    console.log(name);
    return (
        <div>
            
        </div>
    );
};

export default Review;